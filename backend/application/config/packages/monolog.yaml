parameters:
    app.name: '%env(APP_NAME)%'

monolog:
    channels:
        - deprecation

when@dev:
    monolog:
        handlers:
            file_log:
                type: stream
                path: "%kernel.logs_dir%/%kernel.environment%/%app.name%.log"
                level: debug
                channels: ["!event"]
            stream_log:
                type: stream
                path: php://stderr
                level: error
                channels: ["!event"]
                formatter: 'Bref\Monolog\CloudWatchFormatter'
            console:
                type: console
                process_psr_3_messages: false
                channels: ["!event", "!doctrine", "!console"]

when@test:
    monolog:
        handlers:
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                channels: ["!event"]
            nested:
                type: stream
                path: "%kernel.logs_dir%/%kernel.environment%.log"
                level: debug

when@prod:
    monolog:
        handlers:
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                channels: ["!deprecation"]
                buffer_size: 50
            nested:
                type: stream
                path: php://stderr
                level: info
                formatter: 'Bref\Monolog\CloudWatchFormatter'
            console:
                type: console
                process_psr_3_messages: false
                channels: ["!event", "!doctrine"]
            deprecation:
                type: stream
                channels: [deprecation]
                path: php://stderr
                formatter: monolog.formatter.json

services:
    Bref\Monolog\CloudWatchFormatter: ~
