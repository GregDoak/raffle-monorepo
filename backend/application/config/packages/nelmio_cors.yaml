nelmio_cors:
    defaults:
        origin_regex: false
        allow_origin: ['*']
        allow_methods: ['OPTIONS', 'GET', 'POST']
        allow_headers: ['Content-Type', 'Authorization', 'API-Version']
        expose_headers: ['Link']
        max_age: 3600
    paths:
        '^/api': null
